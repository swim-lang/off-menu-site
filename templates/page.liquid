<div class="page-shell" style="padding:120px 40px;max-width:820px;margin:0 auto;color:var(--c)">
  <h1 class="serif" style="font-size:clamp(34px,5vw,56px);text-transform:uppercase">{{ page.title }}</h1>
  <div style="margin-top:24px;color:var(--ink)">{{ page.content }}</div>
</div>
